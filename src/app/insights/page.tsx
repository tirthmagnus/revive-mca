import Header from "@/components/Header"; import SiteFooter from "@/components/SiteFooter"; import { ARTICLES } from "@/lib/articles"; import { ArrowUpRight, Clock3 } from "lucide-react";
export default function Insights(){return <><Header/><main className="insights-page"><section className="insights-hero"><div className="site-wrap"><span className="eyebrow">Insights & resources</span><h1>Useful information before the next business decision.</h1><p>Plain-English guides about merchant cash advances, payment pressure, cash flow, and what to prepare before a case review.</p></div></section><section className="site-wrap insights-grid">{ARTICLES.map((a,i)=><a className={`article-card article-card-${i%3}`} key={a.slug} href={`/insights/${a.slug}`}><div className="article-image"><img src={a.image} alt="Business planning"/></div><div className="article-content"><span><Clock3 size={13}/> 5 min read</span><h2>{a.title}</h2><p>{a.excerpt}</p><strong>Read article <ArrowUpRight size={14}/></strong></div></a>)}</section></main><SiteFooter/></>;}
